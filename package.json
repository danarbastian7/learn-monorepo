{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "git": "^0.1.5"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "new-version": "lerna new version --conventional-commits --yes",
    "diff": "lerna diff"
  }
}
